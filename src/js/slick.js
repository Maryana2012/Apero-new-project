// $(document).ready(function(){
//     $('.slider').slick({
//         infinite: true,
//         slidesToShow: 3, // Кількість видимих слайдів одночасно
//         slidesToScroll: 1, // Кількість слайдів, які прокручуються за один раз
//         autoplay: true, // Автоматичне прокручування
//         autoplaySpeed: 200 // Швидкість автоматичного прокручування в мілісекундах
//     });
//   });

  // document.addEventListener('DOMContentLoaded', function() {
  //   $('.slider').slick({
  //       // infinite: true,
  //       slidesToShow: 3, // Кількість видимих слайдів одночасно
  //       slidesToScroll: 1, // Кількість слайдів, які прокручуються за один раз
  //       autoplay: true, // Автоматичне прокручування
  //       autoplaySpeed: 2500 // Швидкість автоматичного прокручування в мілісекундах
  //   });
  // });

  